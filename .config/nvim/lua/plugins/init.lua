-- This is for simple plugins that don't need much configuration

return {
	"nvim-lua/plenary.nvim", -- lua functions that many plugins use
	"lambdalisue/vim-suda", -- Let's you write as sudo when you forgot to open a file as sudo. ":SudaWrite"
}
