-- the reason why this works without putting the "lua" folder first is
-- a mistery to me, but at this point I just want things to work -_-
require("core.options")
require("core.keymaps")
require("lazy.lazy")
